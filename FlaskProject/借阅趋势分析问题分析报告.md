# 借阅趋势分析功能问题分析报告

## 问题定位

通过对代码的全面审查，我发现了导致借阅趋势分析结果不准确的以下关键问题：

### 1. 数据采集逻辑问题

**问题**：当数据库中没有实际借阅记录时，API返回的是硬编码的模拟数据，而不是基于实际数据的分析结果。

**代码位置**：`routes/stats.py:134-141`

```python
if not dates:
    if interval == 'day':
        dates = [(end_date - timedelta(days=i)).strftime('%Y-%m-%d') for i in range(30, 0, -1)]
        borrow_counts = [42, 38, 55, 48, 62, 35, 28, 45, 52, 61, 38, 29, 47, 53, 41, 36, 58, 44, 49, 56, 63, 37, 32, 51, 46, 59, 42, 39, 54, 48]
        return_counts = [int(c * 0.85) for c in borrow_counts]
    elif interval == 'month':
        dates = ['2026-01', '2026-02', '2026-03', '2026-04']
        borrow_counts = [1250, 1480, 1620, 985]
        return_counts = [1062, 1258, 1377, 837]
```

### 2. 统计方法问题

**问题**：归还量数据不是基于实际的归还记录计算，而是通过固定比例（85%）从借阅量推导，这与实际情况不符。

**代码位置**：`routes/stats.py:131`

```python
return_counts = [int(count * 0.85) for count in borrow_counts]
```

### 3. 时间维度处理问题

**问题**：
- 不同时间间隔（hour、day、month）的数据格式不一致
- 对于月份和小时的处理使用了 `date_format` 函数，可能导致时区问题
- 缺少对时间区间的完整覆盖，可能导致数据缺失

**代码位置**：`routes/stats.py:93-115`

### 4. 异常值处理问题

**问题**：
- 没有处理数据异常值和缺失值
- 没有考虑假期、特殊事件等因素对借阅模式的影响
- 没有数据验证和质量控制机制

### 5. 数据导入问题

**问题**：
- 数据导入过程中可能存在日期时间格式处理问题
- 没有看到 `borrow_records.xlsx` 的完整导入逻辑
- 可能存在数据导入不完整的情况

## 优化方案

### 1. 数据采集逻辑优化

**方案**：
- 移除硬编码的模拟数据，改为基于实际数据库查询结果
- 对于不同时间维度，使用统一的数据结构和格式
- 确保数据覆盖完整的时间区间，包括没有借阅记录的日期

**实施步骤**：
1. 修改 `get_borrow_trend` 函数，移除硬编码数据
2. 实现完整的时间区间生成逻辑
3. 确保返回的数据格式一致

### 2. 统计方法优化

**方案**：
- 基于实际的归还记录计算归还量
- 实现更准确的统计方法，考虑借阅周期和归还率
- 添加数据验证和质量控制机制

**实施步骤**：
1. 修改 `get_borrow_trend` 函数，添加归还记录的查询
2. 实现基于实际数据的归还量计算
3. 添加数据质量检查和异常值处理

### 3. 时间维度处理优化

**方案**：
- 统一不同时间维度的数据格式
- 处理时区问题，确保数据一致性
- 实现完整的时间序列处理

**实施步骤**：
1. 统一时间格式处理
2. 实现时区一致性处理
3. 添加时间序列完整性检查

### 4. 异常值处理优化

**方案**：
- 添加异常值检测和处理机制
- 考虑特殊事件对借阅模式的影响
- 实现数据质量监控

**实施步骤**：
1. 添加异常值检测算法
2. 实现数据质量评估机制
3. 添加数据清洗和预处理步骤

### 5. 数据导入优化

**方案**：
- 确保 `borrow_records.xlsx` 数据的完整导入
- 优化日期时间格式处理
- 添加数据导入验证

**实施步骤**：
1. 检查并完善 `borrow_records.xlsx` 的导入逻辑
2. 优化日期时间格式处理
3. 添加导入数据验证

## 具体代码修改建议

### 1. 优化 `get_borrow_trend` 函数

```python
@stats_bp.route('/borrow-trend', methods=['GET'])
def get_borrow_trend():
    """获取借阅趋势数据（用于折线图）"""
    period = request.args.get('period', 'week')

    end_date = datetime.utcnow()

    if period == 'day':
        start_date = end_date - timedelta(days=1)
        interval = 'hour'
    elif period == 'week':
        start_date = end_date - timedelta(days=7)
        interval = 'day'
    elif period == 'month':
        start_date = end_date - timedelta(days=30)
        interval = 'day'
    elif period == 'year':
        start_date = end_date - timedelta(days=365)
        interval = 'month'
    else:
        start_date = end_date - timedelta(days=7)
        interval = 'day'

    # 生成完整的时间区间
    time_points = []
    if interval == 'hour':
        current = start_date.replace(minute=0, second=0, microsecond=0)
        while current <= end_date:
            time_points.append(current)
            current += timedelta(hours=1)
    elif interval == 'day':
        current = start_date.replace(hour=0, minute=0, second=0, microsecond=0)
        while current <= end_date:
            time_points.append(current)
            current += timedelta(days=1)
    elif interval == 'month':
        current = start_date.replace(day=1, hour=0, minute=0, second=0, microsecond=0)
        while current <= end_date:
            time_points.append(current)
            # 移动到下一个月
            if current.month == 12:
                current = current.replace(year=current.year + 1, month=1)
            else:
                current = current.replace(month=current.month + 1)

    # 查询借阅记录
    borrow_stats = db.session.query(
        func.date_format(BorrowRecord.borrow_date, '%Y-%m-%d %H:00' if interval == 'hour' else '%Y-%m-%d' if interval == 'day' else '%Y-%m').label('time_group'),
        func.count(BorrowRecord.record_id).label('count')
    ).filter(BorrowRecord.borrow_date >= start_date)

    if interval == 'hour':
        borrow_stats = borrow_stats.group_by('time_group').order_by('time_group').all()
    elif interval == 'day':
        borrow_stats = borrow_stats.group_by(func.date(BorrowRecord.borrow_date)).order_by(func.date(BorrowRecord.borrow_date)).all()
    elif interval == 'month':
        borrow_stats = borrow_stats.group_by('time_group').order_by('time_group').all()

    # 查询归还记录
    return_stats = db.session.query(
        func.date_format(BorrowRecord.return_date, '%Y-%m-%d %H:00' if interval == 'hour' else '%Y-%m-%d' if interval == 'day' else '%Y-%m').label('time_group'),
        func.count(BorrowRecord.record_id).label('count')
    ).filter(BorrowRecord.return_date >= start_date, BorrowRecord.return_date.isnot(None))

    if interval == 'hour':
        return_stats = return_stats.group_by('time_group').order_by('time_group').all()
    elif interval == 'day':
        return_stats = return_stats.group_by(func.date(BorrowRecord.return_date)).order_by(func.date(BorrowRecord.return_date)).all()
    elif interval == 'month':
        return_stats = return_stats.group_by('time_group').order_by('time_group').all()

    # 构建时间到计数的映射
    borrow_map = {}
    for stat in borrow_stats:
        key = str(stat.time_group)
        borrow_map[key] = stat.count

    return_map = {}
    for stat in return_stats:
        key = str(stat.time_group)
        return_map[key] = stat.count

    # 构建完整的时间序列数据
    dates = []
    borrow_counts = []
    return_counts = []

    for time_point in time_points:
        if interval == 'hour':
            key = time_point.strftime('%Y-%m-%d %H:00')
        elif interval == 'day':
            key = time_point.strftime('%Y-%m-%d')
        elif interval == 'month':
            key = time_point.strftime('%Y-%m')

        dates.append(key)
        borrow_counts.append(borrow_map.get(key, 0))
        return_counts.append(return_map.get(key, 0))

    return jsonify({
        'dates': dates,
        'borrow_counts': borrow_counts,
        'return_counts': return_counts,
        'period': period,
        'interval': interval
    })
```

### 2. 优化数据导入逻辑

**建议**：确保 `borrow_records.xlsx` 的完整导入，包括正确处理日期时间格式。

### 3. 添加数据质量监控

**建议**：实现数据质量评估机制，包括异常值检测和数据完整性检查。

## 测试验证方法

1. **数据完整性测试**：验证不同时间维度的数据是否完整覆盖
2. **数据准确性测试**：对比API返回的数据与数据库实际记录
3. **性能测试**：测试不同时间范围的数据加载性能
4. **异常值测试**：测试系统对异常数据的处理能力
5. **边界测试**：测试时间范围的边界情况

## 预期效果

通过实施上述优化方案，借阅趋势分析功能将：

1. **准确性提升**：基于实际数据库记录计算，不再使用硬编码的模拟数据
2. **数据完整性**：覆盖完整的时间区间，包括无借阅记录的日期
3. **一致性**：不同时间维度的数据格式一致
4. **可靠性**：具备异常值处理和数据质量监控
5. **可扩展性**：支持更多时间维度和分析维度

这些改进将确保分析结果能够准确反映实际的借阅模式和趋势变化，为图书馆管理决策提供可靠的数据支持。